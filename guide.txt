1. Импорт данных РуТез из xml-файлов.
    bin/rwntools.py import-ruthes-xml -s <xml source dir>
2. Исправление и очистка данных.
    Выполнить запросы из db/001.sql
3. Импорт дополнительных отношений.
    Скрипты из scripts.
    scripts/import_class-instance_relations.py запускать три раза
    Запросы и проверки из
4. Генерация lex-файлов.
    sql2lex/sql2lex.py
    tar -czf lex-lite-$(date +%F).tgz ./*.all
5. Конвертация данных РуТез → RuWordNet.
    sql2sql/sql2sql.py
6. Генерация xml-файлов RuWordNet.
    bin/rwntools.py generate-rwn-xml --out-dir=var/out/rwn-xml -v
    cd var/out/rwn-xml
    tar -czvf rwn-xml-$(date +%F).tgz ./*
7. Обновление БД сайта.
    dump_name=ruthes_db-$(date +%F).sql
    ruwordnet_host=159.203.117.22
    pg_dump ruthes > $dump_name
    perl -pi -w -e 's/german/ruwordnet/g;' $dump_name
    tar -czf $dump_name.tgz $dump_name
    scp $dump_name.tgz ruwordnet@${ruwordnet_host}:
    ssh ruwordnet@${ruwordnet_host} "tar -xzf $dump_name.tgz;\
        sudo -u postgres psql ruthes -c 'drop schema public cascade; create schema public;';\
        sudo -u postgres psql ruthes < $dump_name"
